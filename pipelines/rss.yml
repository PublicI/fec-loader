---
pipeline:
  name: fec-raw-rss-filing-check
transform:
  image: publicintegrity/fec-loader:1eeddaa19e44
  cmd:
  - bash
  stdin:
  - ./bin/rss2fec | head -n 100 | while read url; do touch "/pfs/out/$(echo $url | tr -dc '0-9').fec"; done
parallelism_spec:
  constant: 1
input:
  cron:
    name: tick
    spec: "*/1 * * * *"
