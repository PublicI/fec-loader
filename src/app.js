var sync = require('./util/sync');
//    express = require('express');

sync(function (err) {
    if (err) throw err;

/*
    var app = express();

    app.use(express.static(__dirname + '/../../data/fec/sheets/'));

    app.listen(8080);*/
});
